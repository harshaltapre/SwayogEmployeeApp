import { defineConfig } from "@prisma/internals";

export default defineConfig({
  // Configuration for Prisma v7
  // Database connection URLs are now passed via environment variables
  // and configured in the PrismaClient constructor
});
